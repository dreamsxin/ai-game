# 小队枪战 · 3v3 团队射击

手机触屏优先的俯视角团队射击对战。你带两个队友打三个敌人，自由方向移动、双摇杆瞄准，
枪口有惯性、散布随状态变化，视线内的敌人会被软锁牵引，先到 15 分或三分钟结束时领先的一方获胜。

## 玩法

- 触屏：左半屏按下拖动是移动摇杆，任意方向自由跑；右半屏是瞄准摇杆，轻推只转枪口，推过一半自动开火。
- 键盘鼠标：`WASD` / 方向键移动，鼠标指向瞄准，左键开火，右键或 `Shift` 端稳，`R` 换弹，`Esc` / `P` 暂停。
- 瞄准辅助分两层：视线内、辅助锥内、13 格内的敌人会成为软锁目标，枪口被额外朝它牵引；偏角进锁定锥后准星和射线变金色，才算真咬住。
- 散布决定子弹落点：站定最准，跑动加惩罚，每开一枪抬枪，端稳会把散布整体收到三分之一，代价是脚步降到 55% 速度、枪口转得更慢。
- 伤害按距离衰减，9 格内满伤，18 格外只剩 55%；掩体挡子弹，队友不挡（没有友伤）。
- 地图是 20×24 的竖版对称掩体场，我方从下方三个点出生、敌方从上方出生，横向长廊把掩体串成可绕行的路线。
- 弹匣 25 发，打空自动换弹，换弹 1.6 秒内扣扳机无效。阵亡 3 秒后在离敌人最远的出生点复活。
- 计分：击杀给队伍加 1 分，玩家个人分按伤害和连杀累计，连杀每层多给 40 分。

## 反馈

射击游戏里「自己有没有在开枪、子弹有没有打中人、刚才那下是谁在挨枪」本来是靠耳朵判断的，
此前这一局全程静默，全靠盯屏幕上那几个小圈。现在这几件事各有一声，全部由 WebAudio 现场合成。

- **我的枪和别人的枪是两个音色**：我的干脆靠前，别人的闷、远、轻到只剩轮廓。
  模拟层的 `shot` 事件带 `mine` 就是为了分这一刀。
- **打中人（`mark`）和被打中（`hurt`）在音区两端**，一个短亮一个闷坠，绝不会听混。
  这两条也靠新加的 `mine` / `taken` 分流——原来的 `hit` 只有 `team`，是「谁开的枪」而不是「谁挨的枪」。
- **连杀越串越高**：`frag` 按 `STREAK_SHIFTS` 移调，第六杀封顶。`kill` 事件带的 `streak`
  是击杀者结算之后的值，所以第一杀就是基音。
- **阵亡声压过挨枪声**：致命那一枪同时发 `hit` 和 `kill`，阵亡本身已经交代了「你挨的这下是最后一下」。
  但如果同一帧里我也换掉了一个，两声都会有。
- **换弹分三声**：主动换弹（机械两记）、打空被迫换弹（更硬更刺）、上膛完成（清亮咔嗒）。
  最后一条是那 1.6 秒空窗期结束的唯一凭证。
- **限流**：单枪间隔只有 0.12 秒，但场上有 6 个人，别人的枪声叠起来每秒有 40 多条。
  `THROTTLE` 给 `shot` 和 `impact` 挂了最小间隔，关键反馈（我的枪、命中、挨枪、击杀、上膛）一条都不限。
  这一层放在引擎里而不是 `soundsFor` 里，因为它跨帧才成立。
- **触感**只给和自己性命相关的三件事：阵亡、挨枪、拿人头。枪声全程震手会麻。
- 顶部可静音，偏好写进 `localStorage`；静音开局连 `AudioContext` 都不建。
- 结算面板：破纪录挂徽章（拿开局那一刻的旧纪录比，不是拿已被本局刷过的 `best` 比），
  下面一句 `rewardLabel` 说清这一局赢在哪、输在哪；`prefers-reduced-motion` 下动效全停。

## 本地运行

```bash
npm run dev:xiaodui-qiangzhan   # http://127.0.0.1:4185
npm run test:xiaodui-qiangzhan
npm run build:xiaodui-qiangzhan
```

## 结构

`src/game/` 是纯逻辑层，不碰 DOM：`rules.js` 集中场地尺寸、枪械数值、散布与计分公式，
`arena.js` 是掩体地图、圆形碰撞、分轴滑墙、采样射线与视线判定，
`aim.js` 是这款游戏的核心——枪口惯性（`turnToward`）、软锁选靶（`acquireTarget`）、牵引（`steerAim`）与准星读数，
`combat.js` 推进子弹并做线段-圆命中，`bots.js` 把「看得见谁→拉扯距离→顶完反应时间开枪」写成和玩家输入同构的意图，
`input.js` 把键盘、鼠标和双摇杆归一成同一份输入，`simulation.js` 是定步长状态机，`layout.js` 是渲染与输入共用的换算。

`src/scene/` 是表现层，不写回模拟状态：`render.js` 用 Canvas 2D 画掩体、单位朝向、弹道、散布锥、
锁定圈和双摇杆，`readout.js` 派生 HUD 文案，`audio.js` 里 `SOUNDS`／`soundsFor` 是纯数据与纯函数
（能在 node 里单测），只有 `createAudio` 碰 `AudioContext`，且第一次真要出声时才建。
`render.js` 的 `notify` 只收 `LIFE` 表里有的事件类型——换弹这类纯听觉事件没有坐标，
混进粒子池会落到兜底分支里画出 NaN 圈。

玩家和机器人走同一条推进路径（意图 → `stepUnit`），随机只来自 seed 驱动的 PRNG，
所以同一个 seed 加同一串输入结果逐字段一致，测试不需要浏览器。
`test/playthrough.test.js` 直接拿机器人的意图当玩家输入代打整局，数值一旦失衡（打不中人、一边倒、卡进墙里）就会被测出来。
